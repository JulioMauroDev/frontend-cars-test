import AppWrapper from 'components/AppWrapper';
import CarsPage from 'pages/Cars';

const App = (props) => {
  return (
    <AppWrapper>
      <CarsPage />
    </AppWrapper>
  );
};

export default App;
